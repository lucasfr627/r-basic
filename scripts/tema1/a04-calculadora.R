2*(3+5/2)

2/3+2

725%/%7

##averiguar algoritmo de euclides

pi

2*pi

NA ## NOT AVAILABLE

Inf

c(2^30,2,2^50)


sqrt(25)

exp(1)

factorial(4)

abs(-pi)

factorial(0)

choose(5,3) ## combinatoria

## coeficientes binomiales
## triangulo de pascal


## trigonometria
#### 


cos(60*pi/180) ## conversion a radianes

sin(60*pi/180) ## conversion a radianes

sinpi(1/2) ## = sin (pi/2)

tan(pi)

tan(pi/2)

acos()

asin(0.8660254) * 180 / pi

asin(5)



print(x,n)
round(x,n) ## redondeo por cantidad de decimales
floor(x) ## parte entera por defecto de x
ceiling(x) ## parte entera por exceso de x
trunc(x) ## nos quedamos con entero


print(sqrt(2),10)
round(sqrt(2),2)
floor(sqrt(2))
ceiling(sqrt(2))
trunc(sqrt(2))

sqrt(2)^2-2


2^50
print(2^50,15)



print(pi,22)


#### R NO DEBE MANEJAR PRECISION DE DECIMALES


?round

round(sqrt(2),0)
round(sqrt(2))
round(digits = 5,sqrt(2))





X = (pi^2)/2
X

y <- cos(pi/4)
y

edad <- 30

nombre = "Lucas Rodriguez"
pi.4 = 4*pi
x=x*2

### funcion f(x) = x^3 - (3^x)

f = function(x){x^3 - (3^x)* sin(x)}
f(4)


product <- function(x,y){x*y}
product(4,2)

g<- function(x,y,z){
  exp(x^2 + y^2) + sin(z)
}

g(1,2,3)
g(1,-1,pi)

ls()
rm(product)
rm(list = ls())
